<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sunsh.smartlayout">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OverScrollActivity" />
        <activity android:name=".SmartActivity" />
        <activity android:name=".over.ListViewActivity" />
        <activity android:name=".over.RecyclerViewActivity" />
        <activity android:name=".over.NesteScrollViewActivity" />
        <activity android:name=".over.WebViewActivity" />
        <activity android:name=".smat.WebViewActivity"></activity>
    </application>

</manifest>